Retirement calculator — Spec

  

Add a new tab to the budget calculator for understanding where I stand and what the projected outcome will be based on certain values

  

I needs the totals of what I have now:

1. Total investments from the investments tab
2. Total RA from tab
  

It needs to show the projected income based on a future age i can input and future retirement age based on what I have NOW

The projection is based on my date of birth 08-08-1985 and today's date to calculate future value and a customisable nominal return %

It needs to split out per invest:
- discretionary fund - default 10% return
- TFSA fund - default 10% return
- crypto - default 0% return
- RA fund - default 10%

Then I need a section where I can project what happens if I add more funds. All these are optional should be disabled by default, then option to check them to include
1. Dutch Pension - 900 EURO per month at the age of 68 - translate to ZAR with customisable exchange rate of 1 EUR = 20 ZAR
2. Assume 46000 per year in TFSA with max total of **R500,000**
3. Assume Monthly contribution to RA fund till inputted retirement age - default monthly is R10000 on average
4. Extra value from house sale set default to 2 million ZAR
5. Inheritance in EUR - default = 0. This should instantly be included assuming that the amount is available instantly (so no nominal return assumption etc)



